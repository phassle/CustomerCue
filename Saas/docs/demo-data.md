# Demo data

Concrete UI seeds — they make the demo legible. All explicitly fictional per `../../brief.md`; label as such on-screen.

- **Acme Corp** — $42k ARR, 6 tickets in 14 days about an integration issue, declining sentiment → `churn risk` pre-renewal.
- **NordicPay** — repeated questions about SSO, audit logs, admin roles → `expansion intent`.
- **Onboarding step 3 cluster** — 37 tickets across 11 customers, 4 in target ICP → `onboarding issue` + `bug cluster` for product prioritization.

Use the [canonical signal names](../../docs/signals.md) verbatim. Every demo signal must render with at least one linked source ticket — even if the ticket itself is mocked.
